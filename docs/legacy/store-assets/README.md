# Store assets

- `panel-empty.png`: authentic side-panel capture for the Chrome Web Store.
- `nox-demo.webm`: short authentic UI recording for reviewer context.
- Extension icons are generated from the in-product Nox mark by running
  `powershell -File scripts/generate-icons.ps1`.

Before submission, capture additional connected/read/write states while following
`docs/smoke.md`; use only a scratch workspace with synthetic content. Never commit
tokens, names, or private workspace data.
